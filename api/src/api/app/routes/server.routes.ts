// import express from 'express';
// import { getServer, createServer, getServers, deleteServer, updateServer } from '../controllers/server.controller';

// const serverRoutes = express.Router();

// serverRoutes.route('/')
//   .get(getServers)
//   .post(createServer);

// serverRoutes.route('/:id')
//   .get(getServer)
//   .put(updateServer)
//   .delete(deleteServer);

// export default serverRoutes;